<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Pcci.FailureView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <StackPanel
        Padding="40"
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        MaxWidth="560"
        Spacing="14">
        <FontIcon
            FontSize="40"
            Foreground="{ThemeResource PcciGuessedBrush}"
            Glyph="&#xE7BA;" />
        <TextBlock
            x:Name="MessageText"
            HorizontalAlignment="Center"
            Style="{StaticResource SubtitleTextBlockStyle}"
            TextAlignment="Center"
            TextWrapping="Wrap" />
        <Expander
            x:Name="DetailExpander"
            HorizontalAlignment="Stretch"
            Header="Technical detail">
            <StackPanel Spacing="8">
                <TextBlock
                    x:Name="DetailText"
                    IsTextSelectionEnabled="True"
                    Style="{StaticResource PcciMonospaceTextStyle}"
                    TextWrapping="Wrap" />
                <Button Click="OnCopyDetail" Content="Copy" />
            </StackPanel>
        </Expander>
        <Button
            HorizontalAlignment="Center"
            Click="OnRetry"
            Content="Try again"
            Style="{StaticResource PcciPrimaryButtonStyle}" />
    </StackPanel>
</UserControl>
